% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-lm.R
\name{array_lm}
\alias{array_lm}
\title{Fit linear models with array-shaped response}
\usage{
array_lm(
  X,
  Y,
  weights = NULL,
  intercept = FALSE,
  include_fitted = FALSE,
  include_residuals = FALSE,
  cache = TRUE,
  method = c("normal", "qr"),
  restore_array = TRUE
)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix} of predictors, shape
\code{[n, p]}.}

\item{Y}{Numeric array or matrix of responses. The first dimension must
equal \code{n} (observations). Additional dimensions are treated as
independent response variables.}

\item{weights}{Optional numeric vector of length \code{n} with non-negative
observation weights.}

\item{intercept}{Logical. When \code{TRUE}, a column of ones is prepended
to \code{X} before fitting.}

\item{include_fitted}{Logical. When \code{TRUE}, fitted values are stored
in the returned object.}

\item{include_residuals}{Logical. When \code{TRUE}, residuals are stored
in the returned object.}

\item{cache}{Logical. When \code{TRUE}, the design-matrix factorization is
cached for reuse.}

\item{method}{Solver: \code{"normal"} or \code{"qr"}.}

\item{restore_array}{Logical. When \code{TRUE} (default), \code{rss},
\code{sigma2}, fitted values, and residuals are reshaped to match the
original trailing dimensions of \code{Y}.}
}
\value{
An object of class \code{"am_array_lm_fit"}, a named list
  containing the same fields as \code{\link{many_lm}} plus:
  \describe{
    \item{response_dims}{Integer vector of trailing dimensions of \code{Y}.}
    \item{rss}{Array or vector of residual sums of squares.}
    \item{sigma2}{Array or vector of residual variances.}
  }
}
\description{
Wraps \code{\link{many_lm}} to accept a response \code{Y} with more than
two dimensions (e.g. a 3-D array). The trailing dimensions are collapsed
into columns for fitting and optionally restored in the output.
}
\examples{
X <- matrix(rnorm(50), nrow = 10)
Y <- array(rnorm(10 * 3 * 4), dim = c(10, 3, 4))
fit <- array_lm(X, Y)
dim(fit$sigma2)

}
\seealso{
\code{\link{many_lm}}, \code{\link{lm_fit}}
}
