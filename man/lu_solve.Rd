% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chol-factor.R
\name{lu_solve}
\alias{lu_solve}
\title{Solve a linear system using an LU factor}
\usage{
lu_solve(factor, B)
}
\arguments{
\item{factor}{An \code{\linkS4class{amLU}} object from
\code{\link{lu_factor}}.}

\item{B}{Numeric vector or matrix; the right-hand side. The number
of rows must equal the dimension of \code{factor@A}.}
}
\value{
Numeric vector or matrix \code{x} satisfying
  \code{A \%*\% x == B}. Returns a vector when \code{B} is a
  vector or single-column matrix.
}
\description{
Solves \code{A \%*\% x = B} where \code{A} is the square matrix
stored in \code{factor}, delegating to \code{base::solve} (LAPACK
\code{DGESV}).
}
\examples{
m <- matrix(c(2, 1, 5, 3), nrow = 2)
fac <- lu_factor(m)
b <- c(1, 2)
lu_solve(fac, b)

}
