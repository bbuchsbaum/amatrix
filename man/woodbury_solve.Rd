% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woodbury.R
\name{woodbury_solve}
\alias{woodbury_solve}
\title{Solve a linear system using the Woodbury matrix identity}
\usage{
woodbury_solve(A_factor, U, b, V = NULL, C_inv = NULL)
}
\arguments{
\item{A_factor}{An \code{amChol} object from \code{chol_factor()}, or a
square numeric matrix that is automatically Cholesky-factored.}

\item{U}{Numeric matrix of shape \code{[n, k]}; low-rank left factor.}

\item{b}{Numeric matrix of shape \code{[n, rhs]}; right-hand side(s).}

\item{V}{Numeric matrix of shape \code{[k, n]}; low-rank right factor.
Defaults to \code{t(U)} (symmetric update).}

\item{C_inv}{Numeric matrix of shape \code{[k, k]}; inverse of the
central factor \eqn{C}. Defaults to \code{diag(k)} (pure rank-k
update with \eqn{C = I}).}
}
\value{
Numeric matrix of shape \code{[n, rhs]}: the solution
  \eqn{(A + UCV)^{-1} b}.
}
\description{
Computes \eqn{(A + UCV)^{-1} b} in \eqn{O(nk^2 + k^3)} time using the
Woodbury matrix identity, avoiding an \eqn{O(n^3)} refactorisation of
the updated matrix.
}
\examples{
A <- crossprod(matrix(rnorm(25), 5)) + diag(5)
U <- matrix(rnorm(10), 5, 2)
b <- rnorm(5)
x <- woodbury_solve(A, U, b)
length(x)

}
\seealso{
\code{\link{woodbury_logdet}}, \code{\link{chol_factor}}
}
