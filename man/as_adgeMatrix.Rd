% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{as_adgeMatrix}
\alias{as_adgeMatrix}
\title{Coerce an object to adgeMatrix}
\usage{
as_adgeMatrix(
  x,
  mode = NULL,
  backend = NULL,
  preferred_backend = NULL,
  policy = NULL,
  precision = NULL
)
}
\arguments{
\item{x}{A \code{resident_handle}, base R \code{matrix},
\code{dgeMatrix}, or any \code{denseMatrix}.}

\item{mode}{Single string shortcut; see \code{\link{adgeMatrix}}.}

\item{backend}{Alias for \code{preferred_backend}.}

\item{preferred_backend}{Single string; preferred compute backend.}

\item{policy}{Single string dispatch policy.}

\item{precision}{Single string; \code{"strict"} or \code{"fast"}.}
}
\value{
An \code{adgeMatrix}. When \code{x} is a
  \code{resident_handle} the host copy is deferred.
}
\description{
Converts a matrix-like object or a \code{resident_handle} to an
\code{adgeMatrix}. When \code{x} is a \code{resident_handle},
ownership of the GPU-resident buffer is transferred to the new
\code{adgeMatrix} with no host-side copy.
}
\examples{
m <- matrix(1:6, nrow = 2)
A <- as_adgeMatrix(m)
dim(A)

}
