% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinkhorn.R
\name{sinkhorn}
\alias{sinkhorn}
\title{Doubly-stochastic scaling via Sinkhorn-Knopp iterations}
\usage{
sinkhorn(
  A,
  max_iter = 200L,
  tol = 1e-08,
  check_every = 5L,
  eps = 1e-15,
  mode = "fast",
  backend = NULL,
  return_info = FALSE
)
}
\arguments{
\item{A}{A dense numeric matrix or \code{adgeMatrix}. Sparse inputs are not
yet supported in this surface.}

\item{max_iter}{Maximum number of Sinkhorn iterations.}

\item{tol}{Convergence tolerance on the maximum row/column sum error.}

\item{check_every}{Check convergence every \code{check_every} iterations.}

\item{eps}{Floor applied to row/column sums before division.}

\item{mode}{Execution mode used when coercing a plain matrix. Default
\code{"fast"} allows accelerated backends to use lower precision.}

\item{backend}{Backend name used when coercing a plain matrix. Ignored when
\code{A} is already an \code{adgeMatrix}.}

\item{return_info}{When \code{TRUE}, return convergence metadata alongside
the scaled matrix.}
}
\value{
By default, an \code{adgeMatrix}. With \code{return_info = TRUE}, a
  list containing \code{result}, \code{iterations}, \code{converged},
  \code{row_error}, \code{col_error}, \code{backend}, and \code{method}.
}
\description{
Alternates row and column normalization until the matrix is approximately
doubly stochastic. When the chosen backend supports resident broadcast and
reduction kernels, the hot loop stays on device via \code{resident_handle}
and returns a deferred \code{adgeMatrix} bound to the resident result.
}
\examples{
A <- abs(matrix(rnorm(16), nrow = 4)) + 0.1
S <- sinkhorn(A, max_iter = 50L)
# Row sums should be close to 1
rowSums(as.matrix(S))

}
\seealso{
\code{\link{dist_matrix}}
}
