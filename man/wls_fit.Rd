% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-lm.R
\name{wls_fit}
\alias{wls_fit}
\title{Fit a weighted least squares model}
\usage{
wls_fit(
  X,
  Y,
  weights,
  intercept = FALSE,
  include_fitted = TRUE,
  include_residuals = TRUE,
  cache = TRUE,
  method = c("normal", "qr")
)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix} of predictors, shape
\code{[n, p]}.}

\item{Y}{Numeric matrix, vector, or \code{adgeMatrix} of responses,
shape \code{[n, q]}.}

\item{weights}{Positive numeric vector of length \code{n};
observation weights.}

\item{intercept}{Logical; when \code{TRUE} a column of ones is
prepended to \code{X} before fitting.}

\item{include_fitted}{Logical; include fitted values in the result.}

\item{include_residuals}{Logical; include residuals in the result.}

\item{cache}{Logical; cache intermediate factorizations for reuse
across calls sharing the same \code{X} and \code{weights}.}

\item{method}{Solver method: \code{"normal"} (weighted normal
equations, default) or \code{"qr"} (QR on the weight-scaled
design).}
}
\value{
An object of class \code{"wls_fit"}, a named list
  containing:
  \describe{
    \item{coefficients}{\code{adgeMatrix} of shape \code{[p, q]}.}
    \item{fitted.values}{\code{adgeMatrix} of shape \code{[n, q]},
      or \code{NULL} when \code{include_fitted = FALSE}.}
    \item{residuals}{\code{adgeMatrix} of shape \code{[n, q]}, or
      \code{NULL} when \code{include_residuals = FALSE}.}
    \item{rank}{Integer model rank.}
    \item{df.residual}{Residual degrees of freedom.}
  }
}
\description{
Solves the weighted least-squares problem
\eqn{\min_\beta \sum_i w_i (y_i - x_i^T \beta)^2} using either the
normal equations or a QR decomposition of the weight-scaled design.
}
\examples{
X <- matrix(rnorm(50), nrow = 10)
y <- rnorm(10)
w <- runif(10, 0.5, 2)
fit <- wls_fit(X, y, weights = w)
coef(fit)

}
\seealso{
\code{\link{lm_fit}}, \code{\link{crossprod_weighted}}
}
