% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svd-factor.R
\name{svd_project}
\alias{svd_project}
\title{Project new data onto SVD left singular vectors}
\usage{
svd_project(factor, Y)
}
\arguments{
\item{factor}{An \code{\linkS4class{amSVD}} object from
\code{\link{svd_factor}}.}

\item{Y}{Numeric matrix or vector with \code{nrow(Y)} equal to
the number of rows of the original source matrix.}
}
\value{
Numeric matrix of dimensions \code{k x ncol(Y)} containing
  the projected coordinates.
}
\description{
Computes \code{t(U) \%*\% Y}, where \code{U} is the matrix of left
singular vectors stored in \code{factor}. Routes through a GPU
backend when the factor was computed in \code{"fast"} precision and
a device copy of \code{t(U)} is available.
}
\examples{
m <- matrix(rnorm(30), nrow = 6)
A <- adgeMatrix(m)
fac <- svd_factor(A, k = 2L)
coords <- svd_project(fac, m)

}
