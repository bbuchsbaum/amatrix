% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qr-downdate.R
\name{qr_downdate}
\alias{qr_downdate}
\title{QR downdate after removing one row}
\usage{
qr_downdate(qr_factor, row_idx, X = NULL)
}
\arguments{
\item{qr_factor}{An \code{amQR} factor object returned by
\code{am_qr()}, or any object for which a method is defined.}

\item{row_idx}{Positive integer index of the row to remove.}

\item{X}{The original numeric matrix or \code{adgeMatrix} used to
compute \code{qr_factor}. Required for \code{amQR} factors because
they do not store the source matrix.}
}
\value{
An updated \code{amQR} factor with row \code{row_idx}
  excluded.
}
\description{
Updates a QR factorization to reflect the removal of a single row
from the original matrix. The current implementation refits from the
reduced matrix; a Givens-rotation path is planned for a future
release.
}
\examples{
X <- matrix(rnorm(40), nrow = 8)
qf <- am_qr(X)
qf2 <- qr_downdate(qf, row_idx = 3L, X = X)
qr_info(qf2)$dim

}
\seealso{
\code{\link{am_qr}}, \code{\link{qr_info}},
  \code{\link{lm_loo_cv}}
}
