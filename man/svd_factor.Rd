% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svd-factor.R
\name{svd_factor}
\alias{svd_factor}
\title{Compute a truncated SVD of an aMatrix}
\usage{
svd_factor(
  X,
  k = min(dim(X)),
  method = c("auto", "exact", "rsvd", "subspace"),
  n_oversamples = .amatrix_svd_factor_default_oversamples(k),
  n_iter = 2L
)
}
\arguments{
\item{X}{An \code{aMatrix} (typically \code{adgeMatrix} or
\code{adgCMatrix}).}

\item{k}{Positive integer; the number of singular values and
vectors to compute. Defaults to \code{min(dim(X))}.}

\item{method}{One of \code{"auto"} (default), \code{"exact"},
\code{"rsvd"}, or \code{"subspace"}. \code{"auto"} selects the
algorithm based on matrix dimensions and the available backend.}

\item{n_oversamples}{Non-negative integer; extra random vectors
used to stabilize randomized algorithms. Ignored for
\code{method = "exact"}.}

\item{n_iter}{Non-negative integer; number of power iterations for
the subspace method. Ignored for \code{method = "exact"}.}
}
\value{
An \code{\linkS4class{amSVD}} object containing slots
  \code{u}, \code{d}, \code{v}, and metadata.
}
\description{
Computes a rank-\code{k} truncated singular value decomposition of
\code{X}, dispatching to the most suitable algorithm and backend
based on matrix size, requested rank, and available hardware.
}
\examples{
m <- matrix(rnorm(30), nrow = 6)
A <- adgeMatrix(m)
fac <- svd_factor(A, k = 3L)
fac

}
