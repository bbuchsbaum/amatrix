% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{crossprod_add_diag}
\alias{crossprod_add_diag}
\title{Cross-product plus diagonal perturbation}
\usage{
crossprod_add_diag(X, lambda)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix} of shape \code{[n, p]}.}

\item{lambda}{Scalar or numeric vector of length \code{p}; diagonal
perturbation to add to the cross-product.}
}
\value{
An \code{adgeMatrix} of shape \code{[p, p]}: the perturbed
  cross-product.
}
\description{
Computes \eqn{X^T X + \lambda I} (scalar \code{lambda}) or
\eqn{X^T X + \mathrm{diag}(\lambda)} (vector \code{lambda}) in a
single fused call.
}
\examples{
X <- matrix(rnorm(20), nrow = 5)
crossprod_add_diag(X, lambda = 0.1)

}
\seealso{
\code{\link{crossprod_weighted}}
}
