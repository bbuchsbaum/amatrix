% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-dense.R
\name{determinant.adgeMatrix}
\alias{determinant.adgeMatrix}
\title{Determinant for adgeMatrix}
\usage{
\method{determinant}{adgeMatrix}(x, logarithm = TRUE, ...)
}
\arguments{
\item{x}{An \code{adgeMatrix}.}

\item{logarithm}{Logical; if \code{TRUE}, return the logarithm of the
modulus.}

\item{...}{Further arguments passed to \code{\link[base]{determinant}}.}
}
\value{
A \code{"det"} object with \code{modulus} and \code{sign}.
}
\description{
Computes the determinant of an \code{adgeMatrix} with base matrix semantics.
The method materializes the current host value before delegating to
\code{\link[base]{determinant}} so \code{base::det()} works even when
Matrix is not attached on the search path.
}
\examples{
A <- adgeMatrix(crossprod(matrix(rnorm(9), 3, 3)) + 3 * diag(3))
det(A)

}
\keyword{internal}
