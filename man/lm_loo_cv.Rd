% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qr-downdate.R
\name{lm_loo_cv}
\alias{lm_loo_cv}
\title{Leave-one-out cross-validation for linear models}
\usage{
lm_loo_cv(X, y, method = "qr", ...)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix} of predictors, shape
\code{[n, p]}. No intercept column is added automatically.}

\item{y}{Numeric vector or single-column matrix of responses, length
\code{n}.}

\item{method}{Character string passed to \code{am_qr} controlling
the QR algorithm. Currently only \code{"qr"} is supported.}

\item{...}{Additional arguments forwarded to \code{am_qr}.}
}
\value{
A named list with two elements:
  \describe{
    \item{residuals}{Numeric vector of length \code{n}: LOO prediction
      errors \eqn{y_i - \hat{y}_i^{(-i)}}.}
    \item{mse}{Scalar mean squared LOO error.}
  }
}
\description{
Computes exact leave-one-out (LOO) prediction errors by refitting the
model \code{n} times, each time dropping one observation. Uses
\code{qr_downdate} internally to avoid recomputing the full
factorization from scratch.
}
\examples{
X <- matrix(rnorm(50), nrow = 10)
y <- rnorm(10)
cv <- lm_loo_cv(X, y)
cv$mse

}
\seealso{
\code{\link{lm_fit}}, \code{\link{many_lm}}
}
