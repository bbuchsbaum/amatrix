% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{gemm}
\alias{gemm}
\title{Generalised matrix multiply (BLAS DGEMM interface)}
\usage{
gemm(A, B, C = NULL, alpha = 1, beta = 1, transA = FALSE, transB = FALSE)
}
\arguments{
\item{A}{A matrix or \code{aMatrix}.}

\item{B}{A matrix or \code{aMatrix}.}

\item{C}{Optional matrix or \code{aMatrix} to add after scaling;
\code{NULL} omits the addition term.}

\item{alpha}{Numeric scalar multiplier for \code{op(A) \%*\% op(B)}.
Default \code{1.0}.}

\item{beta}{Numeric scalar multiplier for \code{C}. Default \code{1.0}.}

\item{transA}{Logical; transpose \code{A} before multiplying.
Default \code{FALSE}.}

\item{transB}{Logical; transpose \code{B} before multiplying.
Default \code{FALSE}.}
}
\value{
A matrix of dimensions \code{nrow(op(A))} by
  \code{ncol(op(B))}.
}
\description{
Computes \code{alpha * op(A) \%*\% op(B) + beta * C}, where
\code{op(X) = t(X)} when the corresponding \code{trans} flag is
\code{TRUE}. Routes internally to the most efficient resident
operation for the chosen transpose combination.
}
\examples{
A <- adgeMatrix(matrix(1:6, 2, 3))
B <- adgeMatrix(matrix(1:6, 2, 3))
gemm(A, B, transA = TRUE)          # t(A) \%*\% B
gemm(A, B, transB = TRUE)          # A \%*\% t(B)

}
