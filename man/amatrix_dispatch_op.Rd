% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-planning.R
\name{amatrix_dispatch_op}
\alias{amatrix_dispatch_op}
\title{Low-level backend dispatch for a single operation}
\usage{
amatrix_dispatch_op(x, op, method = op, y = NULL, args = list(), fallback)
}
\arguments{
\item{x}{An \code{aMatrix} object.}

\item{op}{Character string. Operation key used for backend selection
(e.g. \code{"matmul"}, \code{"svd"}).}

\item{method}{Character string. Name of the backend list element to
call. Defaults to \code{op}; override when the backend method name
differs from the operation key.}

\item{y}{Right-hand-side \code{aMatrix} or \code{NULL}. Passed to
the backend method and used during backend selection.}

\item{args}{Named list of additional arguments forwarded to the
backend method on the cold path.}

\item{fallback}{Zero-argument function called when the chosen
backend does not implement \code{method}.}
}
\value{
The result of the backend method, or the result of
  \code{fallback()} if the method is unavailable.
}
\description{
Resolves the best available backend for \code{op} on \code{x},
attempts the GPU-resident path when applicable, and falls back to
the cold path (materializing \code{x} to host) if needed. If the
chosen backend does not implement \code{method}, the \code{fallback}
function is called instead.
}
\seealso{
\code{\link{amatrix_backend_plan}},
  \code{\link{amatrix_materialize_host}}
}
