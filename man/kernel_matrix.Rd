% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{kernel_matrix}
\alias{kernel_matrix}
\title{GPU-accelerated pairwise kernel matrix}
\usage{
kernel_matrix(
  X,
  Y = NULL,
  kernel = c("linear", "rbf", "polynomial", "cosine", "laplacian"),
  sigma = 1,
  degree = 2L,
  coef = 0,
  preferred_backend = NULL,
  zero_diag = FALSE
)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix}, shape [m, p].}

\item{Y}{Numeric matrix or \code{adgeMatrix}, shape [n, p], or \code{NULL}.}

\item{kernel}{Kernel type string (see Details).}

\item{sigma}{Bandwidth for \code{"rbf"} and \code{"laplacian"}.}

\item{degree}{Polynomial degree for \code{"polynomial"}.}

\item{coef}{Constant term for \code{"polynomial"}: \eqn{(\mathrm{coef} + x^\top y)^{\mathrm{degree}}}.}

\item{preferred_backend}{Optional backend name to override the default
dispatch (e.g., \code{"mlx"}, \code{"opencl"}).}

\item{zero_diag}{When \code{TRUE} and \code{Y} is \code{NULL}, set the
diagonal of the kernel matrix to zero.}
}
\value{
Numeric matrix [m, n] of kernel values.
}
\description{
Computes the pairwise kernel matrix between rows of \code{X} and \code{Y}.
The expensive am_tcrossprod is GPU-dispatched; element-wise transforms (exp,
sqrt, pow) run on CPU.
}
\details{
Kernels:
\describe{
  \item{linear}{\eqn{k(x,y) = x^\top y}}
  \item{rbf}{\eqn{k(x,y) = \exp(-\|x - y\|^2 / (2 \sigma^2))}}
  \item{polynomial}{\eqn{k(x,y) = (\mathrm{coef} + x^\top y)^{\mathrm{degree}}}}
  \item{cosine}{\eqn{k(x,y) = (x^\top y) / (\|x\| \|y\|)}}
  \item{laplacian}{\eqn{k(x,y) = \exp(-\|x - y\| / \sigma)}}
}
}
\seealso{
\code{\link{dist_matrix}}
}
