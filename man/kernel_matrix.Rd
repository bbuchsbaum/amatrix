% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{kernel_matrix}
\alias{kernel_matrix}
\title{GPU-accelerated pairwise kernel matrix}
\usage{
kernel_matrix(
  X,
  Y = NULL,
  kernel = c("linear", "rbf", "polynomial", "cosine", "laplacian"),
  sigma = 1,
  degree = 2L,
  coef = 0,
  preferred_backend = NULL,
  zero_diag = FALSE
)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix}, shape [m, p].}

\item{Y}{Numeric matrix or \code{adgeMatrix}, shape [n, p], or \code{NULL}.}

\item{kernel}{Kernel type string (see Details).}

\item{sigma}{Bandwidth for \code{"rbf"} and \code{"laplacian"}.}

\item{degree}{Polynomial degree for \code{"polynomial"}.}

\item{coef}{Constant term for \code{"polynomial"}: (coef + x·y)^degree.}
}
\value{
Numeric matrix [m, n] of kernel values.
}
\description{
Computes the pairwise kernel matrix between rows of \code{X} and \code{Y}.
The expensive am_tcrossprod is GPU-dispatched; element-wise transforms (exp,
sqrt, pow) run on CPU.
}
\details{
Kernels:
\describe{
  \item{linear}{k(x,y) = x·y}
  \item{rbf}{k(x,y) = exp(-||x-y||² / (2σ²))}
  \item{polynomial}{k(x,y) = (coef + x·y)^degree}
  \item{cosine}{k(x,y) = x·y / (||x|| ||y||)}
  \item{laplacian}{k(x,y) = exp(-||x-y|| / σ)}
}
}
\seealso{
\code{\link{dist_matrix}}
}
