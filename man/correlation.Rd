% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-lm.R
\name{correlation}
\alias{correlation}
\title{Compute a correlation matrix}
\usage{
correlation(X, center = TRUE, weights = NULL, block_size = NULL)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix} of shape \code{[n, p]}.}

\item{center}{Logical; when \code{TRUE} (default) column means are
subtracted before computing covariances.}

\item{weights}{Numeric vector of length \code{n}, or \code{NULL}
for unweighted correlation.}

\item{block_size}{Positive integer or \code{NULL}. When non-\code{NULL},
covariances are accumulated in blocks of this many columns to limit
memory usage.}
}
\value{
An \code{adgeMatrix} of shape \code{[p, p]}: the sample
  correlation matrix with diagonal entries set to 1.
}
\description{
Computes the sample correlation matrix of the columns of \code{X},
optionally with observation weights and column-blocked covariance
accumulation.
}
\examples{
X <- matrix(rnorm(50), nrow = 10)
R <- correlation(X)
round(diag(as.matrix(R)), 6)

}
\seealso{
\code{\link{covariance}}
}
