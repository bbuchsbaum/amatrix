% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chol-factor.R
\name{quad_form}
\alias{quad_form}
\title{Evaluate a quadratic form using a Cholesky factor}
\usage{
quad_form(factor, v)
}
\arguments{
\item{factor}{An \code{\linkS4class{amChol}} object from
\code{\link{chol_factor}}.}

\item{v}{Numeric vector or matrix. For a vector, the result is a
scalar; for a matrix with \code{p} columns, the result is a
\code{p x p} matrix.}
}
\value{
Scalar double (when \code{v} is a vector) or numeric matrix
  of dimensions \code{ncol(v) x ncol(v)} containing the quadratic
  form.
}
\description{
Computes \code{t(v) \%*\% solve(X) \%*\% v} (for a vector
\code{v}) or \code{t(V) \%*\% solve(X) \%*\% V} (for a matrix
\code{V}) efficiently via the Cholesky factor of \code{X}, without
forming the inverse.
}
\examples{
m <- crossprod(matrix(rnorm(16), 4, 4)) + diag(4)
A <- adgeMatrix(m)
fac <- chol_factor(A)
v <- rnorm(4)
quad_form(fac, v)

}
