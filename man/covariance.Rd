% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-lm.R
\name{covariance}
\alias{covariance}
\title{Backend-dispatched covariance matrix}
\usage{
covariance(X, center = TRUE, sample = TRUE, weights = NULL, block_size = NULL)
}
\arguments{
\item{X}{Numeric matrix, \code{adgeMatrix}, or sparse \code{adgCMatrix} /
\code{sparseMatrix}, shape \code{[n, p]}.}

\item{center}{Logical. When \code{TRUE} (default), columns are mean-centred
before computing the cross-product.}

\item{sample}{Logical. When \code{TRUE} (default), divides by \code{n - 1}
(sample covariance); when \code{FALSE}, divides by \code{n}.}

\item{weights}{Optional numeric vector of length \code{n} with non-negative
observation weights. When supplied, a weighted covariance is computed.}

\item{block_size}{Optional integer. When set, the cross-product is computed
in column-blocks of this size to limit peak memory. Ignored for sparse
inputs.}
}
\value{
An \code{adgeMatrix} of shape \code{[p, p]}.
}
\description{
Computes the (possibly weighted) sample or population covariance matrix of
\code{X}. Sparse inputs use a memory-efficient path; dense inputs use a
fused GPU kernel when available, otherwise fall back to CPU centering
followed by a GPU cross-product.
}
\examples{
X <- matrix(rnorm(200), nrow = 40)
C <- covariance(X)
dim(C)

}
\seealso{
\code{\link{many_lm}}
}
