% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woodbury.R
\name{woodbury_logdet}
\alias{woodbury_logdet}
\title{Log-determinant via the Woodbury matrix determinant lemma}
\usage{
woodbury_logdet(A_factor, U, V = NULL, C_inv = NULL)
}
\arguments{
\item{A_factor}{An \code{amChol} object from \code{chol_factor()}, or a
square numeric matrix that is automatically Cholesky-factored.}

\item{U}{Numeric matrix of shape \code{[n, k]}; low-rank left factor.}

\item{V}{Numeric matrix of shape \code{[k, n]}; low-rank right factor.
Defaults to \code{t(U)} (symmetric update).}

\item{C_inv}{Numeric matrix of shape \code{[k, k]}; inverse of the
central factor \eqn{C}. Defaults to \code{diag(k)}.}
}
\value{
A length-1 numeric: \eqn{\log|A + UCV|}.
}
\description{
Computes \eqn{\log|A + UCV|} using the matrix determinant lemma
in \eqn{O(nk^2 + k^3)} time, reusing an existing Cholesky factor of
\eqn{A}.
}
\examples{
A <- crossprod(matrix(rnorm(25), 5)) + diag(5)
U <- matrix(rnorm(10), 5, 2)
ld <- woodbury_logdet(A, U)
is.finite(ld)

}
\seealso{
\code{\link{woodbury_solve}}, \code{\link{chol_factor}}
}
