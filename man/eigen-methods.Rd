% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-dense.R
\name{eigen,adgeMatrix-method}
\alias{eigen,adgeMatrix-method}
\alias{eigen,matrix-method}
\title{Eigendecomposition for adgeMatrix}
\usage{
\S4method{eigen}{adgeMatrix}(x, symmetric, only.values = FALSE, EISPACK = FALSE)

\S4method{eigen}{matrix}(x, symmetric, only.values = FALSE, EISPACK = FALSE)
}
\arguments{
\item{x}{An \code{adgeMatrix} or plain \code{matrix}.}

\item{symmetric}{Logical indicating whether \code{x} is symmetric.
When missing, symmetry is detected automatically from the host copy.}

\item{only.values}{Logical; if \code{TRUE} only eigenvalues are returned.}

\item{EISPACK}{Ignored; retained for signature compatibility.}
}
\value{
A list with components \code{values} (numeric vector) and
  \code{vectors} (matrix, omitted when \code{only.values = TRUE}).
}
\description{
Compute eigenvalues and eigenvectors of an \code{adgeMatrix},
dispatching through the amatrix backend for symmetric matrices when
GPU acceleration is available. A fallback method for plain \code{matrix}
preserves base R behaviour after the generic is promoted to S4.
}
\examples{
S <- adgeMatrix(crossprod(matrix(rnorm(9), 3, 3)))
ev <- eigen(S, symmetric = TRUE)
length(ev$values)

}
