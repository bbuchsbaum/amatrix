% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-lm.R
\name{ridge_fit}
\alias{ridge_fit}
\title{Fit a single ridge regression model}
\usage{
ridge_fit(
  X,
  Y,
  lambda,
  intercept = FALSE,
  penalize_intercept = FALSE,
  include_fitted = TRUE,
  include_residuals = TRUE,
  cache = TRUE
)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix} of predictors, shape
\code{[n, p]}.}

\item{Y}{Numeric matrix, vector, or \code{adgeMatrix} of responses,
shape \code{[n, q]}.}

\item{lambda}{Non-negative scalar ridge penalty.}

\item{intercept}{Logical; when \code{TRUE} a column of ones is
prepended to \code{X} before fitting.}

\item{penalize_intercept}{Logical; when \code{FALSE} (default) the
intercept coefficient is excluded from the penalty.}

\item{include_fitted}{Logical; include fitted values in the result.}

\item{include_residuals}{Logical; include residuals in the result.}

\item{cache}{Logical; cache \eqn{X^T X} for reuse across calls
sharing the same \code{X}.}
}
\value{
An object of class \code{"ridge_fit"}, a named list
  containing:
  \describe{
    \item{coefficients}{\code{adgeMatrix} of shape \code{[p, q]}.}
    \item{fitted.values}{\code{adgeMatrix} of shape \code{[n, q]},
      or \code{NULL} when \code{include_fitted = FALSE}.}
    \item{residuals}{\code{adgeMatrix} of shape \code{[n, q]}, or
      \code{NULL} when \code{include_residuals = FALSE}.}
    \item{lambda}{The penalty value used.}
    \item{rank}{Integer model rank.}
    \item{df.residual}{Residual degrees of freedom.}
  }
}
\description{
Solves the penalized least-squares problem
\eqn{\min_\beta \|Y - X\beta\|^2 + \lambda \|\beta\|^2} for a
single penalty value \code{lambda}.
}
\examples{
X <- matrix(rnorm(50), nrow = 10)
y <- rnorm(10)
fit <- ridge_fit(X, y, lambda = 1)
coef(fit)

}
\seealso{
\code{\link{ridge_path}}, \code{\link{lm_fit}}
}
