% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{adgCMatrix}
\alias{adgCMatrix}
\title{Create a backend-aware sparse matrix}
\usage{
adgCMatrix(
  x,
  mode = NULL,
  backend = NULL,
  preferred_backend = NULL,
  policy = NULL,
  precision = NULL
)
}
\arguments{
\item{x}{A \code{dgCMatrix}, other \code{sparseMatrix}, or base R
\code{matrix}.}

\item{mode}{Single string shortcut passed to
\code{.amatrix_resolve_mode()}. Pass \code{NULL} to use the
individual arguments.}

\item{backend}{Alias for \code{preferred_backend}; ignored when
\code{preferred_backend} is non-\code{NULL}.}

\item{preferred_backend}{Single string naming the preferred compute
backend.}

\item{policy}{Single string; one of \code{"auto"}, \code{"cpu"},
\code{"mlx"}, \code{"metal"}, \code{"arrayfire"},
\code{"torch"}.}

\item{precision}{Single string; \code{"strict"} or \code{"fast"}.}
}
\value{
An \code{adgCMatrix} with the data from \code{x} and the
  requested backend metadata.
}
\description{
Converts a sparse or dense matrix to an \code{adgCMatrix} with the
specified backend, policy, and precision. This is the primary
user-facing constructor for sparse amatrix objects.
}
\examples{
m <- matrix(c(1, 0, 0, 2), nrow = 2)
S <- adgCMatrix(m)
S

}
