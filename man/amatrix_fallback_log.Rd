% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-registry.R
\name{amatrix_fallback_log}
\alias{amatrix_fallback_log}
\title{Return the amatrix backend fallback log}
\usage{
amatrix_fallback_log()
}
\value{
A data.frame with columns \code{timestamp}, \code{op},
  \code{from_backend}, \code{to_backend}, \code{reason}. Zero rows
  means no fallbacks have been recorded.
}
\description{
The fallback log records every runtime fall-through from a preferred
backend to the CPU reference path. A non-empty log after a clean
conformance run is a stop-ship condition: it means a backend claimed
support for an op it cannot actually execute, so the result silently
came from a different backend than the one that was requested.
}
\examples{
amatrix_fallback_log()
amatrix_fallback_log_reset()

}
\seealso{
\code{\link{amatrix_fallback_log_reset}},
  \code{\link{amatrix_backend_health_probe}}
}
