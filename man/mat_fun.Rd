% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{mat_fun}
\alias{mat_fun}
\alias{mat_sqrt}
\alias{mat_pow}
\alias{mat_log}
\title{Matrix functions via symmetric eigendecomposition}
\usage{
mat_sqrt(X)

mat_pow(X, p)

mat_log(X)
}
\arguments{
\item{X}{Symmetric positive definite numeric matrix or
\code{adgeMatrix} of shape \code{[p, p]}.}

\item{p}{Numeric scalar exponent (used by \code{mat_pow} only).}
}
\value{
An \code{adgeMatrix} of shape \code{[p, p]}: the matrix
  function applied to \code{X}.
}
\description{
Apply an elementwise function to the eigenvalues of a symmetric
positive definite matrix and reconstruct the result:
\eqn{f(X) = Q \, \mathrm{diag}(f(\lambda)) \, Q^T}.
}
\examples{
S <- crossprod(matrix(rnorm(16), 4)) + diag(4)
mat_sqrt(S)
mat_log(S)
mat_pow(S, -1)

}
