% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{xty_weighted}
\alias{xty_weighted}
\title{Weighted cross-product X'Wy}
\usage{
xty_weighted(X, w, y)
}
\arguments{
\item{X}{Numeric matrix or \code{adgeMatrix} of shape \code{[n, p]}.}

\item{w}{Positive numeric vector of length \code{n}; observation
weights.}

\item{y}{Numeric vector or matrix of shape \code{[n, k]};
response(s).}
}
\value{
An \code{adgeMatrix} of shape \code{[p, k]}.
}
\description{
Computes \eqn{X^T \mathrm{diag}(w) y}, a \code{p x k} weighted
cross-product between \code{X} and response matrix \code{y}. A
GPU-resident fast path is used when available.
}
\examples{
X <- matrix(rnorm(20), nrow = 5)
w <- runif(5)
y <- rnorm(5)
xty_weighted(X, w, y)

}
\seealso{
\code{\link{crossprod_weighted}},
  \code{\link{tcrossprod_weighted}}
}
