% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-dense.R
\name{svd-methods}
\alias{svd-methods}
\alias{svd}
\alias{svd,adgeMatrix-method}
\title{Singular value decomposition for adgeMatrix}
\usage{
\S4method{svd}{adgeMatrix}(x, nu = min(dim(x)), nv = min(dim(x)), LINPACK = FALSE, ...)
}
\arguments{
\item{x}{An \code{adgeMatrix} or plain \code{matrix}.}

\item{nu}{Number of left singular vectors to compute.}

\item{nv}{Number of right singular vectors to compute.}

\item{LINPACK}{Ignored; retained for signature compatibility.}

\item{...}{Further arguments passed to the backend.}
}
\value{
A list with components \code{d} (singular values), \code{u}
  (left singular vectors, \code{nrow(x)} by \code{nu}), and \code{v}
  (right singular vectors, \code{ncol(x)} by \code{nv}).
}
\description{
Compute the singular value decomposition of an \code{adgeMatrix},
dispatching through the amatrix backend. A fallback method for plain
\code{matrix} objects is also provided to preserve base R behaviour
after the generic is promoted to S4.
}
\examples{
A <- adgeMatrix(matrix(rnorm(12), 4, 3))
s <- svd(A)
length(s$d)

}
