% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chol-factor.R
\name{chol_solve}
\alias{chol_solve}
\title{Solve a linear system using a Cholesky factor}
\usage{
chol_solve(factor, B)
}
\arguments{
\item{factor}{An \code{\linkS4class{amChol}} object from
\code{\link{chol_factor}}.}

\item{B}{Numeric vector or matrix; the right-hand side. The number
of rows must equal the dimension of the factor.}
}
\value{
Numeric vector or matrix \code{x} satisfying
  \code{X \%*\% x == B}. Returns a vector when \code{B} is a
  vector.
}
\description{
Solves \code{X \%*\% x = B} where \code{X} is the symmetric
positive-definite matrix whose Cholesky factorization is stored in
\code{factor}. Dispatches to a GPU backend when the factor was
computed in \code{"fast"} precision and a device-resident factor
is available.
}
\examples{
m <- crossprod(matrix(rnorm(16), 4, 4)) + diag(4)
A <- adgeMatrix(m)
fac <- chol_factor(A)
b <- rnorm(4)
x <- chol_solve(fac, b)

}
