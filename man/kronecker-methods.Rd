% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-kronecker.R
\name{kronecker-methods}
\alias{kronecker-methods}
\alias{kronecker,adgeMatrix,adgeMatrix-method}
\alias{kronecker,adgeMatrix,adgCMatrix-method}
\alias{kronecker,adgCMatrix,adgeMatrix-method}
\alias{kronecker,adgCMatrix,adgCMatrix-method}
\alias{kronecker,adgeMatrix,matrix-method}
\alias{kronecker,matrix,adgeMatrix-method}
\alias{kronecker,adgCMatrix,matrix-method}
\alias{kronecker,matrix,adgCMatrix-method}
\title{Kronecker product of backend-aware matrices}
\usage{
\S4method{kronecker}{adgeMatrix,adgeMatrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)

\S4method{kronecker}{adgeMatrix,adgCMatrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)

\S4method{kronecker}{adgCMatrix,adgeMatrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)

\S4method{kronecker}{adgCMatrix,adgCMatrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)

\S4method{kronecker}{adgeMatrix,matrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)

\S4method{kronecker}{matrix,adgeMatrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)

\S4method{kronecker}{adgCMatrix,matrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)

\S4method{kronecker}{matrix,adgCMatrix}(X, Y, FUN = "*", make.dimnames = FALSE, ...)
}
\arguments{
\item{X, Y}{Kronecker factors. At least one is an \code{\linkS4class{aMatrix}}
subclass; the other may be an amatrix, a base \code{matrix}, or a
\pkg{Matrix} object.}

\item{FUN}{Function (or its name) applied to the outer products; passed to
the underlying \pkg{Matrix} method. Defaults to \code{"*"}.}

\item{make.dimnames}{Logical; construct dimnames from the factors. Passed to
the underlying method.}

\item{...}{Further arguments passed to the underlying method.}
}
\value{
An \code{\linkS4class{adgeMatrix}} (dense) or
  \code{\linkS4class{adgCMatrix}} (sparse).
}
\description{
S4 methods for \code{\link[base]{kronecker}} and the \code{\%x\%} operator
that keep the result as an amatrix. Without them, \code{kronecker(A, B)} and
\code{A \%x\% B} dispatch to the \pkg{Matrix} methods for the parent
\code{dgeMatrix} / \code{dgCMatrix} classes and silently demote to a plain
(non-amatrix) result, discarding backend-dispatch metadata.
}
\details{
The product itself is computed by \pkg{Matrix}'s own Kronecker methods on the
materialized host contents, so values are identical to
\code{base::kronecker()} on the dense contents. The result is re-wrapped as
an \code{\linkS4class{adgCMatrix}} when it is sparse and an
\code{\linkS4class{adgeMatrix}} otherwise, inheriting the preferred backend,
policy, and precision of the first amatrix operand.
}
\examples{
A <- adgeMatrix(matrix(1:4, 2, 2))
B <- adgeMatrix(diag(2))
kronecker(A, B)
A \%x\% B

}
