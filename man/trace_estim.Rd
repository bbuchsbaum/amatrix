% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{trace_estim}
\alias{trace_estim}
\title{Stochastic trace estimator (Hutchinson)}
\usage{
trace_estim(A = NULL, k = 30L, seed = NULL, solve_fn = NULL, n = NULL)
}
\arguments{
\item{A}{Square matrix or \code{aMatrix}; required when \code{solve_fn}
is \code{NULL}.}

\item{k}{Integer number of Rademacher probe vectors. Default \code{30L}.}

\item{seed}{Optional integer random seed for reproducibility.}

\item{solve_fn}{Optional function \code{function(V)} that returns
\code{A^{-1} \%*\% V}; use this to estimate \eqn{\mathrm{tr}(A^{-1})}
without materialising the inverse.}

\item{n}{Integer dimension of the matrix; required when \code{solve_fn}
is supplied.}
}
\value{
A single numeric scalar estimate of the trace.
}
\description{
Estimates \eqn{\mathrm{tr}(A)} or \eqn{\mathrm{tr}(A^{-1})} using
Hutchinson's method with \code{k} Rademacher probe vectors. Supply
\code{solve_fn} to estimate the trace of an inverse without forming it
explicitly.
}
\examples{
A <- crossprod(matrix(rnorm(25), 5, 5)) + 5 * diag(5)
trace_estim(A, k = 50L, seed = 1L)

}
