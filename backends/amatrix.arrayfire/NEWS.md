# amatrix.arrayfire 0.1.0

* First tagged release of the ArrayFire backend for `amatrix`.
* Dense float32 linear algebra through the ArrayFire C API with a crash
  firewall around all bridge calls; explicit probe required
  (`amatrix_use_gpu()`).
* Builds with or without the ArrayFire runtime present.
