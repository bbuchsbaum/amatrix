% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logit.R
\name{logit_fit}
\alias{logit_fit}
\title{Logistic regression via IRLS, with amatrix-accelerated kernels}
\usage{
logit_fit(X, y, max_iter = 25L, tol = 1e-08, weight_floor = 1e-10)
}
\arguments{
\item{X}{design matrix: a base \code{matrix}, or any \code{amatrix} dense matrix
(e.g. the result of \code{amatrix::adgeMatrix()}). Should include an
intercept column if one is wanted.}

\item{y}{binary response of length \code{nrow(X)}; values in \{0, 1\}.}

\item{max_iter}{maximum IRLS iterations.}

\item{tol}{convergence tolerance on the relative deviance change
(same criterion as \code{glm}).}

\item{weight_floor}{lower bound applied to the IRLS weights
\eqn{\mu(1-\mu)} for numerical stability near fitted probabilities of
0 or 1.}
}
\value{
an object of class \code{"logit_fit"}: a list with elements
\code{coefficients}, \code{fitted}, \code{deviance}, \code{iter}, \code{converged}.
}
\description{
Fits a binary logistic regression by iteratively reweighted least
squares (IRLS) — the same algorithm that powers
\code{glm(family = binomial())}. The per-iteration bottleneck, forming the
weighted normal equations, is expressed with \code{amatrix} kernels:
}
\details{
\itemize{
\item \code{amatrix::crossprod_weighted(X, w)} — \eqn{X^\top W X}
\item \code{amatrix::xty_weighted(X, w, z)} — \eqn{X^\top W z}
\item \code{solve()} on the resulting \eqn{p \times p} system
}

The function contains \strong{no backend-specific code}. Passed a plain base
matrix it computes on the CPU reference path. To accelerate it, wrap the
design matrix once at the boundary:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{X_gpu <- amatrix::adgeMatrix(X, mode = "fast")   # picks a healthy GPU backend
fit   <- logit_fit(X_gpu, y)
}\if{html}{\out{</div>}}

Everything else — the algorithm, the convergence test, the return value —
is identical. That is the adoption story this package demonstrates.
}
\section{Package-author note}{

\code{\%*\%} and the arithmetic operators dispatch on S4 operands automatically,
but \code{solve()} is a regular function — a package using it on \code{amatrix}
objects must import the method, e.g.
\verb{@importMethodsFrom amatrix solve} (done here in this package).
}

