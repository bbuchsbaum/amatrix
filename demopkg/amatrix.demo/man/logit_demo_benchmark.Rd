% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{logit_demo_benchmark}
\alias{logit_demo_benchmark}
\title{Time the same logit_fit() code on CPU and accelerated inputs}
\usage{
logit_demo_benchmark(n = 20000L, p = 400L, reps = 3L, seed = 1L)
}
\arguments{
\item{n}{number of observations.}

\item{p}{number of predictors (an intercept column is added).}

\item{reps}{timing repetitions; the median is reported.}

\item{seed}{RNG seed for the synthetic data.}
}
\value{
invisibly, a data frame with one row per input type
(\code{backend}, \code{ms}, \code{max_coef_diff_vs_cpu}).
}
\description{
Generates a synthetic logistic-regression problem, fits it with
\code{\link[=logit_fit]{logit_fit()}} on a plain base matrix (CPU reference), then -- if a healthy
GPU backend is installed -- fits the \emph{same} function on an accelerated
\code{adgeMatrix} wrapping of the same data, and reports timings and the
maximum absolute coefficient difference.
}
\details{
The point of the demo: the algorithm code is untouched; only the input
type changes.
}
